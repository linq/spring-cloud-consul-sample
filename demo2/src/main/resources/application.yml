server:
  port: 8081

spring:
  cloud:
    consul:
      host: localhost
      port: 8500

endpoints:
  health:
    sensitive: false
  restart:
    enabled: true
  shutdown:
    enabled: true

logging:
  level:
    org.springframework.cloud.consul: DEBUG